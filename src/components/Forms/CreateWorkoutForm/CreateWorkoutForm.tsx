import { Button, TextField } from "@mui/material";

export const CreateWorkoutForm = () => {
  // TODO Form still in progress
  return (
    <div>
      <TextField id="standard-basic" label="Workout Title" variant="standard" />
      <Button variant="contained">Add Exercise</Button>
    </div>
  );
};
