import { API_URL } from "./enviroment.constants";

export const USER_REGISTER = `${API_URL}/users/register`;
export const USER_LOGIN = `${API_URL}/users/login`;
export const EXERCISES_FETCH_ALL = `${API_URL}/exercises/all`;
